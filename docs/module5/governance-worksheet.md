# Module 5 Governance Retrospective — Worksheet

Retrospective on what was shared with AI coding tools during this course, and how well
AI-generated output was understood before being accepted.

Risk rubric used below:
- **Low**: public code, course toy project code, no sensitive data, no proprietary logic.
- **Medium**: private but non-sensitive code, internal implementation details, or non-public
  repo context with no secrets and no PII.
- **High**: credentials, tokens, secrets, production config, real customer/user data, regulated
  data, or code not authorized to share.

## 1. What I Shared With AI

| Item shared | Risk | Reason | Safer future version | Ambiguity to resolve |
|---|---|---|---|---|
| Task Tracker code (Modules 2–5) | Low | Entirely synthetic, in-memory-only course toy project — no auth, no real data, no proprietary business logic (per `CLAUDE.md`/`mini-adr.md`). | Already fine as shared; if extra-cautious, paste relative paths (`app/main.py:28`) rather than full local paths (`/home/htantawi/...`) that reveal your machine/username. | None. |
| Test output and stack traces (Modules 2–4) | Ambiguous — likely Low–Medium | Underlying test data is synthetic, but pytest tracebacks routinely embed absolute local paths (e.g. `/home/htantawi/workspace/task-tracker/...`), which is a minor but real info leak (username + directory structure), not a clean Low. | Redact/strip absolute filesystem paths and usernames from tracebacks before pasting (e.g. run with `--tb=short`, or manually trim the path prefix), keeping only the relative path and the actual error. | Did any of these test runs use real (non-synthetic) input data, or did tracebacks expose env vars/hostnames beyond just the local path? If purely synthetic data plus path noise only, this stays Low. |
| Frontend code (Module 3) | Low | Static `frontend/index.html` for the course toy app — no credentials, no backend secrets, no proprietary logic. | Already fine as shared. | None. |
| Dockerfile and CI YAML (Module 4) | Low | Checked both files directly: `Dockerfile` uses only public base images/public pip packages with no hardcoded secrets; `.github/workflows/ci.yml` only checks out code, installs deps, and runs `pytest -v` — no tokens, credentials, or deployment targets present. | Already fine as shared; going forward, grep for `API_KEY`/`TOKEN`/`SECRET`/`PASSWORD`/private-registry URLs before pasting any CI/Docker config, since these are the files where secrets most often get committed by accident. | None — verified against actual file contents. |
| Any real external data used by mistake | Treat as High until characterized | By definition this was real (non-synthetic) external data shared accidentally — that's precisely the High-risk category (real data/content possibly not authorized to share). Per "don't minimize because it's small," unknown real data defaults to the cautious/High end, not a guess at Low. | Can't propose a safe substitute without knowing what it was. General fix going forward: never paste real external data into an AI prompt — use synthetic/fixture data instead — and if it happens again, treat it as an incident (note what/when/where it was shared) rather than just moving on. | Which module this occurred in; what the data actually was (name/email, API response, business doc, etc.) and whether it contained PII, credentials, or third-party proprietary content; whether it was pasted directly into a prompt vs. merely present in in-context files; and whether it's still sitting anywhere in this repo or chat history that needs to be scrubbed. |

## 2. What I Received From AI

| Generated thing | Module | Do I understand it line by line? | Action |
|---|---|---|---|
| Backend models and validators | 2 | Self-assess. Complexity signal: `app/models/__init__.py` (159 lines) has 5 `@field_validator`s (on `name`, `title`, `assignee` ×2) plus one `@computed_field` deriving `is_overdue` from `due_date`/`status` at read time rather than storing it — that computed-field pattern is the one piece worth being able to explain, since it's a design choice, not boilerplate. | If **No/Partial**: trace `is_overdue` by hand for one ToDo and one Done task with a past `due_date`, and explain out loud why each validator raises rather than silently coercing. If **Yes**: no action, spot-check only. |
| Frontend board and drag-and-drop logic | 3 | Self-assess. Complexity signal: `frontend/index.html` is 1250 lines; the drag/drop path alone is ~15 event-driven pieces (`dragstart`/`dragend`/`dragover`/`dragleave`/`drop` handlers plus a `state.dragError` field for surfacing failed moves) wired through vanilla DOM listeners, no framework. | If **No/Partial**: walk the sequence for one drag from `dragstart` → `drop`, and identify what `state.dragError` does when a drop is invalid (e.g., an illegal status transition rejected by the backend). If **Yes**: no action, spot-check only. |
| CI workflow | 4 | Self-assess. Complexity signal: low — `.github/workflows/ci.yml` is a linear 4-step job (checkout → setup Python → install deps → `pytest -v`), no matrix builds, no secrets, no conditional logic. | If **No/Partial**: this is short enough to read fully in one pass — do that rather than skimming. If **Yes**: no action. |
| Dockerfile | 4 | Self-assess. Complexity signal: low-to-moderate — it's a two-stage build (builder installs deps to `/install`, runtime copies them + creates a non-root `app` user). The one non-obvious piece is *why* multi-stage (smaller final image, no build tooling in runtime). | If **No/Partial**: explain why the `useradd`/`USER app` lines exist and what breaks if you drop the multi-stage split. If **Yes**: no action. |
| Security findings and plans | 5 | Self-assess — matters most to not rubber-stamp. `docs/module5/security-review.md` records 6 AI-graded findings (F1–F6) with confirm/reject decisions baked in (e.g., merging F2+F3, downgrading F5/F6). If you can't restate *why* F4 (no auth) was graded "accepted risk" rather than "fix now," that's a gap. | If **No/Partial**: re-derive the F4 and F1 severity reasoning yourself from the code (`app/main.py:28-39`, `README.md:3`) without re-reading the AI's explanation first, then compare. If **Yes**: no action — this row matters most for a governance retrospective, since accepting an AI's risk grading without independently verifying it is itself a governance gap. |
